feat: add sample auto-creation and user type management

- Add automatic sample creation on booking submission and approval
  - Create samples for all service items requiring samples
  - Handle missing samples gracefully with error logging
  - Export createSamplesForBooking function for reuse
  - Create one sample per quantity unit for each service item

- Add user type management feature
  - Add updateUserType API endpoint and mutation hook
  - Add user type dropdown menu in admin users table
  - Allow admins to change user types (MJIIT/UTM/External/Admin)
  - Add visual indicator for current user type in dropdown

- Improve admin notification system
  - Filter admin queries to only active admins
  - Add email field to admin queries for better notification handling
  - Add error handling for notification failures in upload handler
  - Include customer name and email in booking notifications

- Update email redirect documentation
  - Add warnings about EMAIL_REDIRECT_TO usage in production
  - Clarify that all emails are redirected when set

- Format package.json with tabs (cosmetic change)

