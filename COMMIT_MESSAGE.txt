feat: enhance user registration and onboarding flow

- Add comprehensive user registration support
  - Add useRegistrationOptions hook for registration form dropdowns
  - Add user registration repository with createUser function
  - Add user existence checks (by email and auth ID)
  - Add lookup functions for faculty, department, and ikohza validation
  - Support optional institutional/external fields during registration

- Improve onboarding flow validation and UX
  - Make phone number required in both registration and onboarding
  - Add better validation for internal members (academicType, departmentId required)
  - Require ikohzaId for MJIIT members during onboarding
  - Improve company/branch creation with branch name requirement
  - Add validation for branch selection/creation in external user flow
  - Return branchId from createCompany when branch is created

- Enhance onboarding options repository
  - Improve MJIIT detection logic (check code and name patterns)
  - Support branch name parameter in createCompany function
  - Better error messages for missing required fields

- Update registration API to handle full profile creation
  - Support institutional fields (faculty, department, ikohza, academic type)
  - Support external fields (company, branch creation)
  - Automatically determine userType based on faculty selection
  - Handle company/branch creation during registration

- Improve database seeding
  - Add more faculties (Science, Management, Computing)
  - Add departments across all faculties for better testing
  - Add more iKohzas for MJIIT
  - Fix seed cleanup to handle dependencies properly
  - Delete related records before deleting users

- Add error handling improvements
  - Wrap notification calls in try-catch in upload handler
  - Better error messages with field-specific validation details
